version: 2

models:
  - name: fct_payment_reconciliation
    columns:
      - name: order_id
        data_tests:
          - not_null
          - unique

      - name: reconciliation_status
        data_tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - MATCHED
                  - ERROR
